// This file contains default configuration constants for the ulti-mate identicon generator.
// TODO: migrate to a JSON config file if more settings are added in the future.

// Default grid size (nxn)
const DEFAULT_GRID = 5;

// Default cell size in pixels
const DEFAULT_CELL_SIZE = 5;

export { DEFAULT_GRID, DEFAULT_CELL_SIZE };
